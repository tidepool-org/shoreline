language: go

services:
  - mongodb

go:
 - 1.3

install:
 - wget https://raw.githubusercontent.com/tidepool-org/tools/master/come_deps.sh
 - chmod -R 777 ./come_deps.sh

script:
 - source ./build && go test -v ./...